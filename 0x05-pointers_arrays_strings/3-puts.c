#include "holberton"

/**
 * void _puts - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void _puts(char *str)
for (i = 0; s[i] != '\0'; i++)
{
  _putchar('\n');
}
return (i);
