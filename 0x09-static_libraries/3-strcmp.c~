#include "holberton.h"

/**
 * _strcmp -  Holberton School students.
 * @s1: hj
 * @s2: jh
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
  int i;
for (i = 0; s1[i] != s2[i] && s2[i] != '/0'; i++)

if (s1[i] != s2[i])
s1[i] - s2[i];

return (0);
}
