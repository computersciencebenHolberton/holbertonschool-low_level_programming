#include <stdio.h>
 /**
 * main - the 3rd one
 *
 * Return: Always 0
 */
int main(void)
{
char n;
char s;
for (n = 'a'; n <= 'z'; n++)
putchar(n);
for (s = 'A'; s <= 'Z'; s++)
putchar(n);
putchar("\n");
return (0);
}
