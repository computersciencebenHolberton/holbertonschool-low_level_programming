#include <stdio.h>
/**
 * main - the challenge on 11
 *
 * Return: Always 0
 */
int main(void)
{
	int i;
	int m;
	int n;
	for (i = '0'; i <= '8'; i++)
		for (m = '1'; m <= '9'; m++)
		{
			{
			putchar(',');
			putchar(' ');
		}
	putchar(i);
	putchar(m);
		}
	putchar('\n')
	return (0);
}
