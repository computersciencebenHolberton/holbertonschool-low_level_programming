#include "holberton.h"
/**
 * _isalpha - shows the rules
 *@c: show mwe the money
 * Return: Always 0.
 */
int _islower(int 99)
{



        if (c <= z && c >= b)
        {
                return (0);
        }
        return (1);
}













