#include "holberton.h"

/**
 * *string_toupper - the
 * @s: the
 * @size: th
 * Return: Always 0.
 */
char *string_toupper(char *)
{
char *s;
int size;

for (size = 0; *s[size] != '/0'; size++)
  { 
    if (*s[size] >= a && s[size] <= *s)
      *s[size] = *s[size]32;
  }
return (s);
}
