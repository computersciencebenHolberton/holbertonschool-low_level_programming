#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int y;
if (n> 0)

for (y = 10000000000; y > 0 ; y / = 10)
  {
    if (n % 10 != 0)
      _putchar(n / y) % 10)+'0')
if (n > 0)
	n* == 1
	else if (n < 0 )
	  n* == 1;
