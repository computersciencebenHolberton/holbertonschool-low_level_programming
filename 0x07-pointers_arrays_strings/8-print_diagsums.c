#include "holberton.h"
#include <stdio.h>

/**
 * print_diagsums - check.
 *@x: th
 *@y: ti
 * Return: Always 0.
 */
void print_diagsums(int *a, int size);
{
  int x, y;
  for (x = 0; x < 8; x++)
    {
      for (y = 0; x < 8; y++)
	{
	  _putchar(a[x][y];
		   }
	    _putchar('\n');
	}
    }
  return (size);
}
