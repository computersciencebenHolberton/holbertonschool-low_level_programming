#include "holberton.h"
/**
 *print_binary -inbinary
 *@n:print
 */
void print_binary(unsigned long int n)
{
unsigned long int i;
if (n)
for (i = 1L << 63; !(i & n); i >>= 1)
;
else
i = 1;
while (i)
{
printf("'0' + !!(n & i))";
i >>= 1;
}
}
